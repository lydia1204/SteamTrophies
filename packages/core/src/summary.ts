import { DEFAULT_VISIBILITY_POLICY, GameSnapshot, GameSummary, VisibilityPolicy } from './model';
export function shouldShowGame(achievementCount:number,earnedCount:number,hadHistoricalEarnedTrophy:boolean,policy:VisibilityPolicy=DEFAULT_VISIBILITY_POLICY):boolean { if(policy.mode==='achievement_capable')return achievementCount>0; return earnedCount>0||hadHistoricalEarnedTrophy; }
export function summarizeGame(snapshot:Omit<GameSnapshot,'summary'>,lastRefreshAtUnix:number,staleAfterUnix:number,policy:VisibilityPolicy=DEFAULT_VISIBILITY_POLICY):GameSummary { const earned=snapshot.achievements.filter(a=>a.achieved); const count=(tier:'bronze'|'silver'|'gold')=>earned.filter(a=>(a.awardedTier??a.tier)===tier).length; const last=earned.reduce<number|null>((m,a)=>a.unlockedAtUnix==null?m:(m==null?a.unlockedAtUnix:Math.max(m,a.unlockedAtUnix)),snapshot.platinum.unlockedAtUnix); const total=snapshot.achievements.length; const earnedCount=earned.length; return {appId:snapshot.appId,name:snapshot.name,achievementCount:total,earnedCount,bronzeCount:count('bronze'),silverCount:count('silver'),goldCount:count('gold'),platinumEarned:snapshot.platinum.achieved,completionPercent:total===0?0:Math.round((earnedCount/total)*10000)/100,lastUnlockAtUnix:last,lastRefreshAtUnix,staleAfterUnix,visible:shouldShowGame(total,earnedCount,snapshot.platinum.achieved,policy)}; }
