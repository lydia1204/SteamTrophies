import { AchievementRecord, GameSnapshot, TrophyEvent } from './model';
export function deriveEvents(previous:GameSnapshot|undefined,next:GameSnapshot):TrophyEvent[] { const atUnix=next.summary.lastRefreshAtUnix; const out:TrophyEvent[]=[]; const prev=new Map<string,AchievementRecord>(previous?.achievements.map(a=>[a.id,a])??[]); for(const a of next.achievements){if(a.achieved&&!prev.get(a.id)?.achieved)out.push({version:1,type:'achievement_unlocked',atUnix:a.unlockedAtUnix??atUnix,appId:next.appId,achievementId:a.id,tier:a.awardedTier??a.tier,globalUnlockPercent:a.awardedGlobalUnlockPercent});} const historicalCompletion = next.platinum.completionAchievementId && prev.get(next.platinum.completionAchievementId)?.achieved; if(next.platinum.achieved&&!previous?.platinum.achieved&&!historicalCompletion)out.push({version:1,type:'platinum_unlocked',atUnix:next.platinum.unlockedAtUnix??atUnix,appId:next.appId}); out.push({version:1,type:'game_refreshed',atUnix,appId:next.appId,achievementCount:next.summary.achievementCount,earnedCount:next.summary.earnedCount}); return out; }
