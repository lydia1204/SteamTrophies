import { GameSummary, LibraryIndex } from './model';
export function buildLibraryIndex(games:readonly GameSummary[],nowUnix:number):LibraryIndex { const visible=games.filter(g=>g.visible).sort((a,b)=>(b.lastUnlockAtUnix??0)-(a.lastUnlockAtUnix??0)||a.name.localeCompare(b.name)); const totals=visible.reduce((t,g)=>{t.visibleGames++;t.earnedTrophies+=g.earnedCount+(g.platinumEarned?1:0);t.bronze+=g.bronzeCount;t.silver+=g.silverCount;t.gold+=g.goldCount;t.platinum+=g.platinumEarned?1:0;return t;},{visibleGames:0,earnedTrophies:0,bronze:0,silver:0,gold:0,platinum:0}); return {schemaVersion:1,generatedAtUnix:nowUnix,games:visible,totals}; }
